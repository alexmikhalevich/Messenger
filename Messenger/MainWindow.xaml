<Window x:Class="Messenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Messenger" Height="380.484" Width="627.742">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.15*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"/>
        <TextBox Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" IsReadOnly="True"/>
        <TextBox Grid.Column="1" Grid.Row="1"/>
        <Button Grid.Column="2" Grid.Row="1" Content="Send" Click="Button_Click" />

    </Grid>
</Window>
